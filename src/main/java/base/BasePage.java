package base;

import org.openqa.selenium.WebDriver;
import utilities.helpers.WebElementHelpers;

public class BasePage extends WebElementHelpers {

    public BasePage(WebDriver driver) {
        super(driver);
    }
}
